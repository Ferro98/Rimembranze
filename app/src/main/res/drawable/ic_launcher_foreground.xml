<?xml version="1.0" encoding="utf-8"?>
<!--
    Adaptive-icon foreground: only the checkmark glyph, no background circle.
    Bounding box stays well inside the 66dp safe zone (centered in the 108dp
    viewport) so it isn't clipped by circular/squircle/rounded-square launcher
    masks. The background layer (@color/ic_launcher_background) provides the
    amber fill; the launcher's own mask provides the shape.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:pathData="M40,54 L49,63 L72,40"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="7"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:fillColor="#00000000" />
    <path
        android:pathData="M43,77 L65,77"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="6"
        android:strokeLineCap="round"
        android:fillColor="#00000000" />
</vector>
